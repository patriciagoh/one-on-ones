# Supply-chain hardening: do NOT run package lifecycle scripts on install.
# This is the primary defense against npm install-script worms (e.g. Shai-Hulud),
# whose payloads execute via pre/postinstall hooks. If a dependency legitimately
# needs a build script, allowlist it explicitly rather than removing this line.
ignore-scripts=true
